#include <stdio.h>

/**
 * main - Entry point
 *
 * Description :a
 *
 * Return : Always 0 (success)
*/

int main(void)
{
	for (int i = 0; i < 11; i++)
		printf("%i",i);
	printf("\n");

	return (0);
}
